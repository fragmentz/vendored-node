#!/bin/sh

SCRIPT_PATH="$(p="$0"; while l="$(readlink "$p")"; do p="$(dirname "$p")/$l"; done; echo $p)"
SCRIPT_DIR="$(dirname "$SCRIPT_PATH")"
PATH="$SCRIPT_DIR":$PATH exec "$SCRIPT_DIR/_node" "$@"

